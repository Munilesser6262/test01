# クイズサイト 詳細セットアップガイド

このガイドでは、クイズサイトを実行するための詳しい手順を説明します。

## 📋 必要なもの

- Webブラウザ（Chrome、Firefox、Safari、Edgeなど）
- このリポジトリのファイル（index.html、style.css、script.js）

## 🚀 実行方法（詳細版）

### 方法1: ブラウザで直接開く（初心者向け・最も簡単）

**Windows:**
1. エクスプローラーでこのフォルダを開く
2. `index.html` ファイルを見つける
3. `index.html` をダブルクリック
4. 自動的にデフォルトのブラウザで開きます

または：
1. `index.html` を右クリック
2. 「プログラムから開く」を選択
3. お好みのブラウザ（Chrome、Firefox等）を選択

**Mac:**
1. Finderでこのフォルダを開く
2. `index.html` ファイルを見つける
3. `index.html` をダブルクリック
4. 自動的にデフォルトのブラウザで開きます

または：
1. `index.html` を右クリック（または Control + クリック）
2. 「このアプリケーションで開く」を選択
3. お好みのブラウザを選択

**Linux:**
1. ファイルマネージャーでこのフォルダを開く
2. `index.html` ファイルを見つける
3. `index.html` をダブルクリック
4. 自動的にデフォルトのブラウザで開きます

### 方法2: ローカルサーバーで実行（推奨）

ローカルサーバーを使うと、より実際のWebサイトに近い環境でテストできます。

#### Python 3を使う方法

**前提条件:** Python 3がインストールされていること

**確認方法:**
```bash
python3 --version
# または
python --version
```

**実行手順:**

1. ターミナル（Windows: コマンドプロンプトまたはPowerShell、Mac/Linux: ターミナル）を開く

2. このフォルダに移動:
```bash
cd /path/to/test01
# 例: cd ~/Downloads/test01
```

3. サーバーを起動:
```bash
python3 -m http.server 8000
```

Windowsの場合、`python3`が動作しない場合は：
```bash
python -m http.server 8000
```

4. ターミナルに以下のようなメッセージが表示されます：
```
Serving HTTP on 0.0.0.0 port 8000 (http://0.0.0.0:8000/) ...
```

5. ブラウザを開いて、以下のURLにアクセス:
```
http://localhost:8000/
```

6. クイズサイトが表示されます！

7. サーバーを停止するには、ターミナルで `Ctrl + C` を押します

#### Node.jsを使う方法

**前提条件:** Node.jsとnpmがインストールされていること

**確認方法:**
```bash
node --version
npm --version
```

**方法A: http-serverを使う（簡単）**

1. ターミナルを開く

2. このフォルダに移動:
```bash
cd /path/to/test01
```

3. http-serverを実行（インストール不要）:
```bash
npx http-server
```

4. 表示されたURLをブラウザで開く（通常は http://localhost:8080）

**方法B: live-serverを使う（自動リロード付き）**

1. ターミナルを開く

2. live-serverをグローバルインストール（初回のみ）:
```bash
npm install -g live-server
```

3. このフォルダに移動:
```bash
cd /path/to/test01
```

4. live-serverを起動:
```bash
live-server
```

5. 自動的にブラウザが開きます
6. ファイルを編集すると自動的にリロードされます

### 方法3: Visual Studio Code (VS Code)を使う

**前提条件:** VS Codeがインストールされていること

**手順:**

1. VS Codeを起動

2. 「ファイル」→「フォルダーを開く」でこのフォルダを開く

3. 左側のエクスプローラーで `index.html` をクリックして開く

4. 拡張機能「Live Server」をインストール:
   - 左側の拡張機能アイコンをクリック
   - 検索欄に「Live Server」と入力
   - 「Live Server」（作者: Ritwick Dey）をインストール

5. `index.html` を開いた状態で:
   - 右クリック → 「Open with Live Server」を選択
   - または、右下の「Go Live」ボタンをクリック

6. 自動的にブラウザでクイズサイトが開きます

7. ファイルを保存すると自動的にブラウザがリロードされます

8. 停止するには、右下の「Port: 5500」をクリック

### 方法4: オンラインで実行（GitHub Pages）

リポジトリをGitHubにプッシュしている場合：

1. GitHubのリポジトリページに移動
2. 「Settings」タブをクリック
3. 左側のメニューから「Pages」を選択
4. 「Source」で「main」ブランチを選択
5. 「Save」をクリック
6. 数分後、表示されるURLにアクセスすると公開されます

## 🎮 クイズの遊び方

### 起動画面
1. サイトを開くと「クイズゲーム」のタイトルが表示されます
2. 「スタート」ボタンが表示されます

### クイズの進め方
1. **「スタート」をクリック** - 最初の質問が表示されます
2. **答えを選択** - 4つの選択肢から1つをクリック
   - 正解の場合：ボタンが緑色になります ✅
   - 不正解の場合：クリックしたボタンが赤色、正解のボタンが緑色になります ❌
3. **「次へ」をクリック** - 次の質問に進みます
4. **繰り返し** - 全5問を解答します
5. **結果表示** - スコアとパーセンテージが表示されます
   - 100%: 完璧です！🎉
   - 80%以上: 素晴らしい！👏
   - 60%以上: 良くできました！👍
   - 40%以上: もう少しです！💪
   - 40%未満: 頑張りましょう！📚
6. **「もう一度」をクリック** - クイズを最初からやり直せます

## 🔧 トラブルシューティング

### ファイルが開けない
- すべてのファイル（index.html、style.css、script.js）が同じフォルダにあることを確認
- ファイル名が正しいことを確認（大文字・小文字を含む）

### スタイルが適用されない
- style.css ファイルが index.html と同じフォルダにあることを確認
- ブラウザのキャッシュをクリア（Ctrl + Shift + Delete）

### JavaScriptが動作しない
- script.js ファイルが index.html と同じフォルダにあることを確認
- ブラウザのコンソールでエラーを確認（F12を押して「Console」タブを見る）

### Pythonサーバーが起動しない
- Python 3がインストールされているか確認: `python3 --version`
- Windowsの場合は `python` コマンドを試す: `python -m http.server 8000`
- ポート8000が既に使用されている場合は、別のポート番号を指定: `python3 -m http.server 8080`

### ポートが既に使用されている
- エラー: `Address already in use`
- 解決方法: 別のポート番号を使用
```bash
python3 -m http.server 8001
# ブラウザで http://localhost:8001/ を開く
```

## 📱 対応ブラウザ

このクイズサイトは以下のブラウザで動作します：
- Google Chrome（推奨）
- Mozilla Firefox
- Microsoft Edge
- Safari
- Opera

## 💡 ヒント

- スマートフォンやタブレットでも動作します（レスポンシブデザイン）
- インターネット接続は不要です（すべてローカルで動作）
- ファイルを編集してカスタマイズできます：
  - `script.js`: 質問と答えを変更
  - `style.css`: 色やデザインを変更
  - `index.html`: 構造やテキストを変更

## 📞 サポート

問題が解決しない場合は、GitHubのIssueで質問してください。
